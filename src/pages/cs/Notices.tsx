import { PageContainer } from '@ant-design/pro-components';
import { Card } from 'antd';

// Inquiries.tsx 예시
const Notices: React.FC = () => (
  <PageContainer title="1:1 문의 관리">
    <Card>문의 목록 + 답변 달기</Card>
  </PageContainer>
);
export default Notices;
